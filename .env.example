# Copy to .env and fill in, OR just set these in your shell.
# The tool also prompts interactively if these are unset.
# Credentials are only sent to SunStrong's own login endpoint; tokens are never stored.
SUNSTRONG_USERNAME=you@example.com
SUNSTRONG_PASSWORD=your-app-password
